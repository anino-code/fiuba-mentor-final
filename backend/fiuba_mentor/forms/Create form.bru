meta {
  name: Create form
  type: http
  seq: 3
}

post {
  url: http://localhost:3000/api/forms
  body: json
  auth: inherit
}

body:json {
  {
    "id_user": 7,
    "materia": "introCamejo",
    "tema": "el backend",
    "descripcion": "mil vueltas",
    "tipo": "mentor"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
